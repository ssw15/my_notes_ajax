<% publish_to "/notes" do %>

  var new_note = $('<%= j(render @note) %>');

  new_note.hide();

  $('#notes_list').prepend(new_note);

  new_note.slideDown();

<% end %>

$('#note_content').val('');
